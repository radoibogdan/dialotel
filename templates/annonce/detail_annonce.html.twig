{% extends 'base.html.twig' %}

{% block title %}Annonce{% endblock %}

{% block body %}
    <h1>Annonce</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id annonce</th>
                <td>{{ annonce.id }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ annonce.description }}</td>
            </tr>
            <tr>
                <th>Date Annonce</th>
                <td>{{ annonce.dateAnnonce ? annonce.dateAnnonce|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

{#    <a href="{{ path('annonces_list') }}">Retour à la liste</a>#}

    <a class="btn btn-primary js-annonce" data-id-annonce={{ annonce.id }} data-id-user={{ userConnecteId }} href="#" role="button">Offrir mes services</a>
{% endblock %}
